// strings in Ts are declared and used as same as in JS 

// in ts the date type of the varibel are declared along side of the 
// them 

let colour: string = "blue"; 
let fullname: string = 'max min ';
let age: number = 32; 
let sentence: string = `hello my name is ${fullname} and i like ${colour} at the ${age} `
console.log(sentence);